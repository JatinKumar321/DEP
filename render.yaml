services:
  - type: web
    name: dep-midsem-backend
    env: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /
    plan: free
    envVars:
      - key: FRONTEND_ORIGINS
        sync: false
      - key: PORT
        value: "8000"
